export interface ReadFileInfo {
    name: string;
    src: string;
    isCurrent: boolean;
}

export interface PresentInfo {
    thumbnail: string,
    name: string,
    description: string,
    isSecret: boolean
}